# nextjs-hygen-cli
```bash
npm i -g hygen
npm i -g hygen-add
hygen-add https://github.com/Benknightdark/hygen-nextjs-cli
```
```bash
# 英文大小寫轉換
h.camel( str )
h.constant( str )
h.dot( str )
h.header( str )
h.isLower( str )
h.isUpper( str )
h.lower( str )
h.lcFirst( str )
h.no( str )
h.param( str )
h.pascal( str )
h.path( str )
h.sentence( str )
h.snake( str )
h.swap( str )
h.title( str )
h.upper( str )
```
